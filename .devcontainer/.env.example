# Copy to .env (gitignored) and adjust if needed. Dev-only defaults — never reuse
# these values for anything reachable outside your machine.

DB_ROOT_PASSWORD=changeit
ADMIN_PASSWORD=admin
SITE_NAME=bs.local
